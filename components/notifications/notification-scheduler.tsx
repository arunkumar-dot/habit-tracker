"use client";

// This file is intentionally empty — scheduling is handled by NotificationProvider.
// Kept as a named export so existing imports don't break if referenced elsewhere.
export function NotificationScheduler() {
  return null;
}
